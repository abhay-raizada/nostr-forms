export interface ISubmission {
  formId: string;
  submittedAt: string;
  submittedAs: string;
}

export interface ISubmissionTable {
  key: string;
  formName: string;
  submittedAt: string;
  submittedAs: string;
}
